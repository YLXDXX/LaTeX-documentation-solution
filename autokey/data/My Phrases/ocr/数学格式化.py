# Enter script code
import time
#backup = clipboard.get_clipboard()
system.exec_command("~/.config/autokey/myscript/ocr-format/math/process-math.pl")
time.sleep(0.005)  # May need tweaking, up to 0.5
#keyboard.send_keys("<ctrl>+<shift>+v")
#clipboard.fill_clipboard(backup)