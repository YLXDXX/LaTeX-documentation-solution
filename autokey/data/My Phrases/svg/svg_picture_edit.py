# Enter script code
import time
time.sleep(0.02)  # May need tweaking, up to 0.5
ouput = system.exec_command("~/.config/autokey/myscript/svg/svg_picture_edit.sh | echo 123")
time.sleep(0.005)  # May need tweaking, up to 0.5
